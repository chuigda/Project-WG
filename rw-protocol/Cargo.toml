[package]
name = "rw-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.11"
prost-types = "0.11"

[build-dependencies]
prost-build = { version = "0.11", features = ["cleanup-markdown"] }
